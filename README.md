# pg-polygon-orchestr

Библиотека для автоматического развёртывания и управления Docker-контейнерами через Python.

## Требования

- Python 3.14+
- [uv](https://docs.astral.sh/uv/getting-started/installation/) — менеджер зависимостей и виртуальных окружений
- Docker Desktop или Docker Engine — должен быть **запущен** перед выполнением тестов

## Установка

Клонировать репозиторий и установить зависимости:

```bash
git clone <repo_url>
cd pg-polygon-orchestr/project
uv sync
```

## Запуск тестов

> **Важно:** интеграционные тесты запускают реальные Docker-контейнеры и требуют работающего Docker daemon.

Запуск всех интеграционных тестов:

```bash
uv run pytest -m integration -vv
```

Запуск конкретного теста:

```bash
uv run pytest -m integration -k <имя_теста>
```

Например:

```bash
uv run pytest -m integration -k test_1__two_nodes_deployed_started_named_and_destroyed
```

## Конфигурация

| Файл | Назначение |
|---|---|
| `pytest.ini` | Конфигурация pytest (маркеры, пути к тестам) |
| `pyproject.toml` | Метаинформация проекта и зависимости |

## Настройка линтинга и статического анализа

Разработка ведётся в VSCode со следующими расширениями:
- `Python` (линтинг, дебаг и.т.д)
- `Black Formatter`

`Black Formatter` применяется автоматически при сохранении файла

`Pylance` настроен в режиме строгой проверки типов.

## Возможные проблемы

**После прерывания тестов остались мусорные контейнеры:**

Тесты могут оставить контейнеры, образы и сети если были прерваны принудительно (Ctrl+C)

Контейнеры именуются так, как вы именуете ноды, то же правило действует для сетей и томов

Для образов имена (репозиторий) имеют следующий формат `image_node_[имя ноды]`

## Очистка мусора

Docker при выполнении команд pull (когда вы скачиваете образ из Docker Hub) скачивает промежуточные слои образа (чаще всего вы их видите как `<none>:<none>`)

Для интеграционного тестирования или ваших тестов с использованием библиотеки Docker скорее всего будет обращаться к `Docker Hub` за новыми образами и ваша файловая система docker-образов будет наполняться промежуточными none'ами

Со временем некоторые none'ы станут неиспользованными промежуточными слоями (мусором). Их стоит очистить чтобы не засорять диск - для этого используйте команду:

```bash
docker rmi $(docker images -f "dangling=true" -q)
```

Подробнее можно почитать, к примеру, в этой статье: https://habr.com/ru/articles/304134/