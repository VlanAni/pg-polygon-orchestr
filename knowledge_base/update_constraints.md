https://docs.docker.com/reference/cli/docker/container/update/

## Команда `update`

Эта команда позволяет обновлять конфигурацию контейнера (ограничения на память, процессор, как делаем рестарт контейнера)

В dockerSDK есть аналог - `update()`

Команда `update()` не работает на `Windows`-контейнерах